export const tooltipConfig = {
  // Verzögerung in Millisekunden, bevor ein Tooltip angezeigt wird
  delayDuration: 400,

  // Verzögerung in Millisekunden, wenn der Benutzer zwischen Tooltip-Triggern wechselt
  skipDelayDuration: 200,
}
